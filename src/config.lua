-- Default configuration values.
-- Users can override these via Chalk (config.lua in plugins_data).
return {
    Enabled              = true,
    DebugMode            = false,
    CharybdisBehavior    = false,
    EscalatingFigLeaf    = false,
    SkipGemBossReward    = false,
    SurfaceStructure     = false,
}
